name: vsd_mcp
description: An MCP server for reading statmon files.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.8

dependencies:
  mcp_dart: ^2.1.1
  path: ^1.9.0
  vsd_core:
    path: ../core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  vsd_mcp:
